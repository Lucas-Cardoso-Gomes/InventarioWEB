@model Web.Models.RedeDetailsViewModel

@{
    ViewData["Title"] = "Detalhes do Ativo de Rede";
}

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-hdd-network"></i> @ViewData["Title"] - @Model.Rede.Nome</h2>
        <div>
            @if (User.IsInRole("Admin"))
            {
                <a asp-action="Edit" asp-route-id="@Model.Rede.Id" class="btn btn-warning"><i class="bi bi-pencil"></i> Editar</a>
            }
            <a asp-action="Index" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Voltar</a>
        </div>
    </div>

    <ul class="nav nav-tabs" id="redeTabs" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active" id="dados-tab" data-bs-toggle="tab" data-bs-target="#dados" type="button" role="tab" aria-controls="dados" aria-selected="true"><i class="bi bi-info-circle"></i> Informações do Ativo</button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="trocas-tab" data-bs-toggle="tab" data-bs-target="#trocas" type="button" role="tab" aria-controls="trocas" aria-selected="false"><i class="bi bi-clock-history"></i> Histórico de Trocas</button>
        </li>
    </ul>

    <div class="tab-content border border-top-0 p-4 bg-white" id="redeTabsContent">

        <!-- Tab: Informações -->
        <div class="tab-pane fade show active" id="dados" role="tabpanel" aria-labelledby="dados-tab">
            <div class="row">
                <div class="col-md-6">
                    <div class="card mb-3">
                        <div class="card-header bg-light"><i class="bi bi-diagram-3"></i> <strong>Rede e Identificação</strong></div>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item"><strong>ID:</strong> @Model.Rede.Id</li>
                            <li class="list-group-item"><strong>Nome:</strong> @Model.Rede.Nome</li>
                            <li class="list-group-item"><strong>Tipo:</strong> @Model.Rede.Tipo</li>
                            <li class="list-group-item"><strong>IP:</strong> @Model.Rede.IP</li>
                            <li class="list-group-item"><strong>MAC Address:</strong> @Model.Rede.MAC</li>
                        </ul>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card mb-3">
                        <div class="card-header bg-light"><i class="bi bi-geo-alt"></i> <strong>Localização e Garantia</strong></div>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item"><strong>Localização:</strong> @Model.Rede.Localizacao</li>
                            <li class="list-group-item"><strong>Endereço:</strong> @Model.Rede.Endereco</li>
                            <li class="list-group-item"><strong>Garantia:</strong> @(Model.Rede.DataGarantia.HasValue ? Model.Rede.DataGarantia.Value.ToString("dd/MM/yyyy") : "Não definida")</li>
                        </ul>
                    </div>
                </div>
                <div class="col-md-12">
                     <div class="card mb-3">
                        <div class="card-header bg-light"><i class="bi bi-pencil-square"></i> <strong>Observações</strong></div>
                        <div class="card-body">
                            <p class="card-text">@(string.IsNullOrEmpty(Model.Rede.Observacao) ? "Sem observações." : Model.Rede.Observacao)</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Tab: Histórico de Trocas -->
        <div class="tab-pane fade" id="trocas" role="tabpanel" aria-labelledby="trocas-tab">
             @if (Model.HistoricoTrocas != null && Model.HistoricoTrocas.Any())
            {
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover align-middle">
                        <thead class="table-dark">
                            <tr>
                                <th>Data da Alteração</th>
                                <th>Campo Alterado</th>
                                <th>Valor Antigo</th>
                                <th>Valor Novo</th>
                                <th>Usuário</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var h in Model.HistoricoTrocas)
                            {
                                <tr>
                                    <td>@(DateTime.TryParse(h.DataAlteracao, out var dt) ? dt.ToString("dd/MM/yyyy HH:mm:ss") : h.DataAlteracao)</td>
                                    <td>@h.CampoAlterado</td>
                                    <td>@h.ValorAntigo</td>
                                    <td>@h.ValorNovo</td>
                                    <td>@h.Usuario</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-info">Nenhum histórico de troca registrado para este equipamento.</div>
            }
        </div>

    </div>
</div>
