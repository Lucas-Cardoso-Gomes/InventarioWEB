@model byte[]

@{
    ViewData["Title"] = "Screen Capture";
}

@if (Model != null)
{
    <img src="data:image/png;base64,@Convert.ToBase64String(Model)" />
}
else
{
    <p>Failed to capture screenshot.</p>
}
